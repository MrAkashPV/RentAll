specVersion: "v1beta"
serviceId: "rentalapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rentalapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
